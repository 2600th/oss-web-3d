node_modules/
dist/
.vite/
.DS_Store
*.local

# Large upstream art source. Only the optimized public/models/mig21.glb is
# tracked; regenerate it with `npm run optimize:model` when this is present.
assets/source/

# Agent execution checkpoint (see PROMPT.md section 6)
.agent/
