node_modules/
dist/
.vite/
.DS_Store
*.local

# Large upstream art source. Only the optimized public/models/mig21.glb is
# tracked; drop the original here and run `npm run optimize:model` to
# regenerate it.
assets/source/

# Local scratch: development notes, captures and probe scripts.
.agent/
.superpowers/
